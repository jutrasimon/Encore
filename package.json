{
  "name": "encore-band-roguelite",
  "version": "0.9.2",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "dev": "python3 -m http.server 8000 --directory dist",
    "check": "node --check dist/engine.js && node --check dist/app.js && node --check dist/network.js && node --check dist/resolution.js && node --check dist/resolution-audio.js && node --check dist/stage-audio.js && node --check dist/juice.js && node --check dist/autoplay.js && node --check server/room.js && node --check server/http.js"
  }
}
